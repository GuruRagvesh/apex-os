// Core Organization — departments frontend surface.
//
// The two route-level screens. There are no sub-components to expose: the
// Avatar helper, the colour maps and the modals are all inline in the screens,
// exactly as they were in the route files.

export { default as DepartmentsScreen } from './screens/DepartmentsScreen';
export { default as DepartmentDetailScreen } from './screens/DepartmentDetailScreen';
