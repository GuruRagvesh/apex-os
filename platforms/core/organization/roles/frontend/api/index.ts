// Core Organization Roles — HTTP API surface.
//
// Reached only as '@apex/core-organization-roles/api'. Declared as an exact
// public entry in architecture-boundaries.json: any deeper path into this
// folder is private and rejected.
//
// Deliberately separate from the component root barrel.

export { rolesApi } from './roles-api';
