// Core Users — change-requests frontend surface.
//
// One route-level screen. There are no sub-components to expose: the reject
// modal, the loading state and both constant tables are inline in the screen,
// exactly as they were in the route file.

export { default as ApprovalsScreen } from './screens/ApprovalsScreen';
