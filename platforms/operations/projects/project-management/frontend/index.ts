// Operations Projects — project-management frontend surface.
//
// Only the two route-level screens are published. There are no sub-components
// to expose: both screens are self-contained today.

export { default as ProjectsScreen } from './screens/ProjectsScreen';
export { default as ProjectDetailScreen } from './screens/ProjectDetailScreen';
