// Workforce Calendar — overview frontend surface.
//
// One route-level screen. CalendarView stays inline in it, exactly as it was in
// the route file, so the FullCalendar bundles keep loading lazily.

export { default as CalendarScreen } from './screens/CalendarScreen';
