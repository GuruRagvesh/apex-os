// Sales CRM — settings component public entry point.
//
// This is CRM-scoped configuration only: CRM fields, CRM role permissions,
// integrations and CRM data import/backup. It is NOT the Apex OS global
// settings route, and it carries no auth, workday, attendance or scheduler
// dependency.

export * from './frontend';
