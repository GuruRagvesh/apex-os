// Sales CRM — shell component public entry point.
//
// Owns the CRM workspace chrome: sidebar, topbar and the planned-feature pane.
// shell.module.css and tokens.module.css are component-owned and imported by
// relative path from siblings, preserving their relative cascade order.

export * from './frontend';
