// Sales CRM — database component public entry point.
//
// Stylesheets are never re-exported here: database.module.css is
// component-owned and imported by relative path from its siblings, and shared
// stylesheets arrive by exact path from sales-crm/shared.

export * from './frontend';
