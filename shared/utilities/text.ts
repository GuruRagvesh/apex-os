// Text helpers.
//
// Moved verbatim from frontend/lib/utils.ts.

export function getInitials(name: string): string {
  return name
    .split(' ')
    .map((n) => n[0])
    .join('')
    .toUpperCase()
    .slice(0, 2);
}
