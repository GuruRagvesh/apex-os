'use client';

// TicketRow lives in the tickets module — import from here instead of components/tickets/ticket-row
// Re-exports the canonical implementation for module boundary compliance.
export { TicketRow } from '@/components/tickets/ticket-row';
