{
  "$schema": "https://json.schemastore.org/base.json",
  "version": 1,
  "description": "Apex OS vertical-slice architecture dependency boundaries. Consumed by scripts/architecture/validate-boundaries.mjs. Phase 0: enforcement covers only the new architecture roots; frontend/, backend/ and e2e/ are legacy-active and enter enforcement as their modules migrate.",
  "documentation": "docs/architecture/PUBLIC_API_CONVENTIONS.md",

  "enforcedRoots": ["apps", "platforms", "database", "shared"],

  "legacyActiveRoots": {
    "roots": ["frontend", "backend", "e2e"],
    "enforced": false,
    "reason": "These are the live application roots serving production. They are excluded from enforcement until their modules migrate, per Phase 0 scope."
  },

  "platforms": ["core", "workforce", "operations", "intelligence", "business", "system"],

  "componentAliases": {
    "description": "Short public aliases for migrated components, mirroring frontend/tsconfig.json paths. Declared here so the validator RESOLVES them instead of treating them as bare packages — otherwise an import reaching into a component's internals through its alias would be invisible to the rules below. Each alias must address a component root, or a declared public subpath listed in publicSubpaths.",
    "@apex/sales-crm-leads": "platforms/business/sales-crm/leads",
    "@apex/sales-crm-shared": "platforms/business/sales-crm/shared",
    "@apex/sales-crm-shared/styles": "platforms/business/sales-crm/shared/frontend/styles",
    "@apex/sales-crm-shared/api": "platforms/business/sales-crm/shared/frontend/api",
    "@apex/shared-auth": "shared/auth",
    "@apex/operations-projects": "platforms/operations/projects/project-management",
    "@apex/operations-projects/screens": "platforms/operations/projects/project-management/frontend/screens",
    "@apex/intelligence-dashboard": "platforms/intelligence/dashboard/overview",
    "@apex/shared-ui": "shared/ui",
    "@apex/shared-ui/components": "shared/ui/frontend/components",
    "@apex/workforce-leave": "platforms/workforce/leave/applications",
    "@apex/shared-utilities": "shared/utilities",
    "@apex/core-users": "platforms/core/users/administration",
    "@apex/core-users/screens": "platforms/core/users/administration/frontend/screens"
  },

  "sharedModulePublicEntry": {
    "description": "Modules under shared/ that have migrated real code and therefore publish a public entry point. Anything outside the module may import the module root or its index file only — reaching into its internal frontend/, backend/, shared/ or tests/ folders is a boundary violation, exactly as for a platform component. Only modules listed here are enforced; the remaining shared/ folders are still Phase 0 scaffolds with nothing to protect.",
    "modules": ["shared/auth", "shared/ui", "shared/utilities"]
  },

  "publicSubpaths": {
    "description": "Entry points that are PUBLIC despite living under a component-internal folder, for things that must not travel through the component's root barrel. Two reasons arise in practice. (1) A CSS module re-exported through a JavaScript barrel changes position in the bundle graph and therefore its cascade order against other stylesheets applied to the same elements. (2) An HTTP API re-exported through the root barrel drags axios and the authenticated-client module into every consumer of that barrel, including screens that only want types or constants. Matching is on the ORIGINAL IMPORT SPECIFIER, not on the resolved path: several spellings can resolve to the same file, and only the canonical alias is public. '@apex/sales-crm-shared/api' passes; '@apex/sales-crm-shared/frontend/api/sales-crm-leads-api' addresses the same code through the component's internal folder and is REJECTED. Each entry maps the exact permitted specifier to the exact repository path it must resolve to — if the two disagree the import is rejected, so a drifting alias in frontend/tsconfig.json cannot silently widen this. Exact specifiers only: never a wildcard, never a directory prefix.",
    "specifiers": {
      "@apex/sales-crm-shared/styles/primitives.module.css": "platforms/business/sales-crm/shared/frontend/styles/primitives.module.css",
      "@apex/sales-crm-shared/api": "platforms/business/sales-crm/shared/frontend/api",
      "@apex/operations-projects/screens/ProjectsScreen": "platforms/operations/projects/project-management/frontend/screens/ProjectsScreen",
      "@apex/operations-projects/screens/ProjectDetailScreen": "platforms/operations/projects/project-management/frontend/screens/ProjectDetailScreen",
      "@apex/shared-ui/components/breadcrumb": "shared/ui/frontend/components/breadcrumb",
      "@apex/shared-ui/components/empty-state": "shared/ui/frontend/components/empty-state",
      "@apex/shared-ui/components/user-avatar": "shared/ui/frontend/components/user-avatar",
      "@apex/shared-ui/components/staging-banner": "shared/ui/frontend/components/staging-banner",
      "@apex/shared-ui/components/AnnouncementBroadcast": "shared/ui/frontend/components/AnnouncementBroadcast",
      "@apex/shared-ui/components/CommandModal": "shared/ui/frontend/components/CommandModal",
      "@apex/shared-ui/components/QuickActionPalette": "shared/ui/frontend/components/QuickActionPalette",
      "@apex/shared-ui/components/CommandCard": "shared/ui/frontend/components/CommandCard",
      "@apex/shared-ui/components/HoverPreview": "shared/ui/frontend/components/HoverPreview",
      "@apex/shared-ui/components/KpiCapsuleStrip": "shared/ui/frontend/components/KpiCapsuleStrip",
      "@apex/shared-ui/components/KpiCapsule": "shared/ui/frontend/components/KpiCapsule",
      "@apex/shared-ui/components/skeleton": "shared/ui/frontend/components/skeleton",
      "@apex/shared-ui/components/multi-select": "shared/ui/frontend/components/multi-select",
      "@apex/shared-ui/components/status-badge": "shared/ui/frontend/components/status-badge",
      "@apex/core-users/screens/UsersScreen": "platforms/core/users/administration/frontend/screens/UsersScreen",
      "@apex/core-users/screens/UserDetailScreen": "platforms/core/users/administration/frontend/screens/UserDetailScreen"
    }
  },

  "layers": {
    "apps": {
      "path": "apps",
      "mayImport": ["platforms", "shared", "database"],
      "mustNotImport": [],
      "notes": "Composition shells only. No business rules. One app must not import another app's internals."
    },
    "platforms": {
      "path": "platforms",
      "mayImport": ["platforms", "shared", "database"],
      "mustNotImport": ["apps"],
      "notes": "Cross-component and cross-platform imports must use a public index entry point."
    },
    "shared": {
      "path": "shared",
      "mayImport": ["shared", "database"],
      "mustNotImport": ["apps", "platforms"],
      "notes": "Cross-cutting only. Importing a platform inverts the dependency graph."
    },
    "database": {
      "path": "database",
      "mayImport": ["database"],
      "mustNotImport": ["apps", "platforms", "shared"],
      "notes": "Bottom of the graph. Schema, migrations and the Prisma client import nothing internal."
    }
  },

  "rules": [
    {
      "id": "frontend-no-backend",
      "severity": "error",
      "description": "Frontend code must not import backend code.",
      "from": "**/frontend/**",
      "forbiddenTargetSegments": ["backend"],
      "rationale": "Server code must never reach the browser bundle."
    },
    {
      "id": "backend-no-frontend",
      "severity": "error",
      "description": "Backend code must not import frontend code.",
      "from": "**/backend/**",
      "forbiddenTargetSegments": ["frontend"],
      "rationale": "Server runtime must not depend on browser modules."
    },
    {
      "id": "shared-no-platforms",
      "severity": "error",
      "description": "shared/ must not import apps/ or platforms/.",
      "fromLayer": "shared",
      "forbiddenLayers": ["apps", "platforms"],
      "rationale": "shared/ is depended upon by platforms; importing back creates a cycle."
    },
    {
      "id": "shared-no-legacy-frontend",
      "severity": "error",
      "description": "shared/ must not import the legacy frontend/ or backend/ roots.",
      "fromLayer": "shared",
      "forbiddenLayers": ["frontend", "backend"],
      "rationale": "shared/ sits below every platform, so depending on legacy feature code inverts the graph exactly as importing a platform does. Unlike platforms/, there is no exemption mechanism: shared/ is new code with no migration history to carry, and shared/auth must stay importable from a Nest process where frontend/ does not exist. Reported as shared-no-legacy-frontend or shared-no-legacy-backend."
    },
    {
      "id": "database-no-upper-layers",
      "severity": "error",
      "description": "database/ must not import apps/, platforms/ or shared/.",
      "fromLayer": "database",
      "forbiddenLayers": ["apps", "platforms", "shared"],
      "rationale": "The Prisma client must be importable from every layer without a cycle."
    },
    {
      "id": "platforms-no-apps",
      "severity": "error",
      "description": "platforms/ must not import apps/.",
      "fromLayer": "platforms",
      "forbiddenLayers": ["apps"],
      "rationale": "Apps compose platforms, never the reverse."
    },
    {
      "id": "app-internal-isolation",
      "severity": "error",
      "description": "An app must not import another app's internal files.",
      "scope": "apps",
      "rationale": "Apps are independent composition shells."
    },
    {
      "id": "component-public-entry",
      "severity": "error",
      "description": "Cross-component imports must target a component's public entry point, not its internal frontend/, backend/, shared/ or tests/ folders.",
      "scope": "platforms",
      "internalSegments": ["frontend", "backend", "shared", "tests"],
      "componentDepth": 3,
      "rationale": "A component's internals are private; only index.ts is public."
    }
  ],

  "legacyFrontendImports": {
    "description": "platforms/** importing frontend/** is FORBIDDEN by default. Migration phases may carry a narrow, temporary, per-component exemption listed below. Each entry names exactly one source component and an explicit allowlist of destination paths. platforms/** -> backend/** has no exemption mechanism and is always forbidden.",
    "defaultRule": "forbidden",
    "exceptions": [
      {
        "id": "DEBT-P6-CORE-USERS-LEGACY-FRONTEND",
        "status": "temporary",
        "introducedIn": "Core Users administration frontend compartmentalisation",
        "removalPhase": "Per-target: see removalConditions",
        "from": "platforms/core/users/administration/frontend/",
        "allowedTargets": [
          "frontend/lib/api.ts",
          "frontend/lib/utils.ts",
          "frontend/store/auth.store.ts"
        ],
        "reason": "The two admin screens moved verbatim and carry legacy imports across three targets. None is Core Users-owned: lib/api.ts holds application-wide API groups (usersApi, rolesApi, departmentsApi, ticketsApi, leaveApi, projectsApi, workdayApi, dashboardApi); lib/utils.ts supplies the ticket-owned STATUS_COLORS and PRIORITY_COLORS used to render a user's ticket list; auth.store.ts is the application-wide identity store. Note this component administers users but does NOT own identity - the auth store belongs to core/identity, and moving it here would invert that. Both screens already consume cn/formatDate/getInitials from @apex/shared-utilities, so that coupling is already retired.",
        "removalConditions": [
          "frontend/store/auth.store.ts: the auth store migrates to platforms/core/identity and is consumed through its public entry",
          "frontend/lib/utils.ts: STATUS_COLORS and PRIORITY_COLORS move with operations/tickets",
          "frontend/lib/api.ts: usersApi becomes a Core Users-owned adapter once an app-wide API layer is compartmentalised"
        ]
      },
      {
        "id": "DEBT-P5-LEAVE-LEGACY-FRONTEND",
        "status": "temporary",
        "introducedIn": "Workforce Leave frontend compartmentalisation",
        "removalPhase": "Per-target: see removalConditions",
        "from": "platforms/workforce/leave/applications/frontend/",
        "allowedTargets": [
          "frontend/lib/api.ts",
          "frontend/lib/utils.ts",
          "frontend/store/auth.store.ts"
        ],
        "reason": "The Leave screen moved verbatim and carries three legacy imports. None is Leave-owned: leaveApi lives in frontend/lib/api.ts among ~20 application-wide API groups; lib/utils.ts is a broad shared module (cn, formatDate, getInitials) that also holds LEAVE_STATUS_COLORS; auth.store.ts is the application-wide identity store. The screen's global UI dependency was already retired - it consumes EmptyState through @apex/shared-ui/components/empty-state, not the legacy path.",
        "removalConditions": [
          "frontend/lib/utils.ts: LEAVE_STATUS_COLORS splits out to this component (cn/formatDate/getInitials already moved to shared/utilities)",
          "frontend/store/auth.store.ts: the auth store migrates to platforms/core/identity",
          "frontend/lib/api.ts: leaveApi becomes a Leave-owned adapter once an app-wide API layer is compartmentalised"
        ]
      },
      {
        "id": "DEBT-P4-DASHBOARD-LEGACY-FRONTEND",
        "status": "temporary",
        "introducedIn": "Intelligence Dashboard frontend compartmentalisation",
        "removalPhase": "Per-target: see removalConditions",
        "from": "platforms/intelligence/dashboard/overview/frontend/",
        "allowedTargets": [
          "frontend/lib/api.ts",
          "frontend/lib/company-date.ts",
          "frontend/store/auth.store.ts",
          "frontend/components/workday/"
        ],
        "reason": "The dashboard screen and its eight widgets moved verbatim and carry legacy imports across six target groups. None is dashboard-owned: lib/api.ts holds application-wide API groups (ticketsApi, dashboardApi, changeRequestsApi); lib/utils.ts and lib/company-date.ts are broad shared helpers; auth.store.ts is the application-wide identity store; components/workday/ (WorkdayBar, WorkdayHistoryStrip) belongs to workforce/attendance and is explicitly out of scope for this phase. Moving any of them would restructure unrelated shared code or touch the attendance engine.",
        "removalConditions": [
          "frontend/components/workday/: workforce/attendance is compartmentalised and its bar is consumed through a public entry",
          "frontend/lib/company-date.ts: migrates to shared/time per D1",
          "frontend/store/auth.store.ts: the auth store migrates to platforms/core/identity",
          "frontend/lib/api.ts: dashboardApi becomes dashboard-owned once an app-wide API layer is compartmentalised"
        ]
      },
      {
        "id": "DEBT-P3-PROJECTS-LEGACY-FRONTEND",
        "status": "temporary",
        "introducedIn": "Operations Projects frontend compartmentalisation",
        "removalPhase": "Per-target: see removalConditions",
        "from": "platforms/operations/projects/project-management/frontend/screens/",
        "allowedTargets": [
          "frontend/lib/api.ts",
          "frontend/lib/utils.ts",
          "frontend/store/auth.store.ts",
          "frontend/components/tickets/ticket-row.tsx"
        ],
        "reason": "The two Projects screens moved verbatim and carry 9 legacy imports across 6 targets. None of the six is Projects-owned, so none could follow: projectsApi has 5 consumers of which 4 are not Projects (tickets/new, users/[id], profile, command-palette); lib/utils.ts is a broad shared module (cn, formatDate, getInitials) that also happens to hold the PROJECT_STATUS_* constants; auth.store.ts is the application-wide identity store; TicketRow is owned by operations/tickets and has three consumers. Splitting any of them would restructure unrelated shared code, which this phase explicitly does not do. The exemption is scoped to the screens folder only.",
        "removalConditions": [
          "frontend/lib/api.ts: projectsApi becomes a Projects-owned adapter once core/users and operations/tickets stop consuming it, or once an app-wide API layer is compartmentalised",
          "frontend/lib/utils.ts: PROJECT_STATUS_LABELS and PROJECT_STATUS_COLORS split out to this component (cn/formatDate/getInitials already moved to shared/utilities)",
          "frontend/store/auth.store.ts: the auth store migrates to platforms/core/identity and is consumed through its public entry",
          "frontend/components/tickets/ticket-row.tsx: operations/tickets is compartmentalised and TicketRow is consumed through its public entry"
        ]
      },
      {
        "id": "DEBT-P2B-LEADS-COMPANY-REPOSITORY",
        "status": "temporary",
        "introducedIn": "Phase 2B",
        "removalPhase": "Phase 2D (Sales CRM company/database ownership)",
        "from": "platforms/business/sales-crm/leads/frontend/components/CompanyAutocomplete.tsx",
        "allowedTargets": ["frontend/lib/sales-crm/company/company-repository.ts"],
        "reason": "Phase 2B moved CompanyAutocomplete into the Leads component because Leads is its only consumer. Its company lookup still calls LocalStorageCompanyRepository, which could not follow: the repository has a second importer (frontend/lib/sales-crm/storage.ts) and it reads MOCK_CLIENTS from frontend/lib/sales-crm/database-data.ts, which is owned by the unmigrated Sales CRM database component. Moving the repository into Leads would take a database-owned dependency with it; moving it into the shared component would make shared depend on a feature's mock data. Both invert ownership, so the repository stays where it is until the company/database concern gets an owner.",
        "removalConditions": [
          "The Sales CRM database component migrates, giving database-data.ts an owner",
          "LocalStorageCompanyRepository and its normalization helper move to that owner or to the Sales CRM shared component",
          "CompanyAutocomplete consumes it through a public entry point"
        ]
      },
      {
        "id": "DEBT-P2C-LEADS-GLOBAL-USERS-API",
        "status": "temporary",
        "introducedIn": "Phase 2C",
        "removalPhase": "Phase 3 (core/users platform slice)",
        "from": "platforms/business/sales-crm/leads/frontend/screens/SalesCrmLeads.tsx",
        "allowedTargets": ["frontend/lib/api.ts"],
        "reason": "Phase 2C moved salesCrmLeadsApi out of frontend/lib/api.ts and eliminated 5 of the 6 imports it carried. The sixth line imported two symbols: salesCrmLeadsApi AND usersApi. usersApi is an application-wide API group used by 20 files across attendance, tickets, projects and admin; it is not a Sales CRM concern and must not move into a Sales CRM component. The Leads screen calls usersApi.getAll() once, to populate the assignable lead-owner list. Re-implementing GET /users inside Sales CRM would duplicate an existing application API to satisfy a boundary, which is worse than recording the dependency.",
        "removalConditions": [
          "core/users migrates to platforms/core/users with a public entry point",
          "The Leads screen resolves assignable owners through that entry point"
        ]
      },
      {
        "id": "DEBT-P2A-SALES-CRM-AUTH-STORE",
        "status": "temporary",
        "introducedIn": "Phase 2A",
        "removalPhase": "Phase 3 (identity platform slice)",
        "from": "platforms/business/sales-crm/shared/frontend/api/auth-adapter.ts",
        "allowedTargets": ["frontend/store/auth.store.ts"],
        "reason": "The Sales CRM auth adapter maps an Apex identity onto a CRM Role. It is the single point of contact between Sales CRM and the application-wide Zustand auth store, which is owned by the identity platform and must not move in a Sales CRM phase. Every other Sales CRM file consumes this adapter, never the store.",
        "removalConditions": [
          "The application-wide auth store migrates to platforms/core/identity/",
          "The adapter imports it through that component's public entry point"
        ]
      }
    ]
  },

  "publicEntryPoints": {
    "filenames": ["index.ts", "index.tsx", "index.js", "index.mjs"],
    "componentPathPattern": "platforms/<platform>/<module>/<component>",
    "notes": "A cross-component import may address the component directory itself or its index file, but must not address a path inside it."
  },

  "scan": {
    "extensions": [".ts", ".tsx", ".js", ".jsx", ".mjs", ".cjs"],
    "ignoreDirectories": [
      "node_modules",
      "dist",
      "build",
      ".next",
      "coverage",
      "__fixtures__",
      "fixtures",
      ".git"
    ],
    "ignoreFilePatterns": [".d.ts"]
  }
}
